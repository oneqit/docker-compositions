# Kafka secure mode
# Note: Kafka credentials are configured in kafka_jaas.conf
# Default: admin / your-secure-password-here

# Kafka UI (secure mode)
KAFKA_SECURITY_PROTOCOL=SASL_PLAINTEXT
KAFKA_SASL_MECHANISM=PLAIN
KAFKA_SASL_JAAS_CONFIG=org.apache.kafka.common.security.plain.PlainLoginModule required username="admin" password="your-secure-password-here";
