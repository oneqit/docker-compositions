services:
  redis:
    image: redis:7-alpine
    container_name: oneqit_redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass your-secure-password-here
    networks:
      - oneqit-redis-network
    restart: unless-stopped

networks:
  oneqit-redis-network:
    driver: bridge
